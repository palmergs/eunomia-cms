{{#each model.contentBlocks as |contentBlock|}}
  {{content-editor model=contentBlock 
      onClick=(action "blockSelected") 
      onDelete=(action "contentDeleted")
      onCompleted=(action "contentCompleted") }}
{{/each}}
{{yield}}
